{
  "name": "ai-dm-bot-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "dashboard",
    "ai-dm-bot"
  ],
  "scripts": {
    "build": "cd dashboard && npm install && npm run build",
    "install-all": "npm install && cd dashboard && npm install && cd ../ai-dm-bot && npm install"
  }
}
